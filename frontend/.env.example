# ── API ────────────────────────────────────────────────────────────────────────
# Local development: point directly to the backend
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Production on Vercel: leave empty so all /api/* calls go through the Vercel rewrite
# NEXT_PUBLIC_API_URL=""
